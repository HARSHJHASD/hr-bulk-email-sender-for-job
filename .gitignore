# Dependencies
node_modules/

# Credentials — never commit
.env

# Runtime data: real HR addresses + send history
data/*.jsonl
recipients*.csv
recipients*.txt

# Campaign archives: subject/body + real HR addresses
data/campaigns/

# Uploaded / dropped attachments (keep the folder, not its contents)
attachments/*
!attachments/.gitkeep

# Logs / OS cruft
*.log
.DS_Store
